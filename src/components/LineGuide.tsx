"use client";

/**
 * FOUNDATION STUB — milik Person B (bulan 1-2).
 * Reading ruler overlay. Person B implements cursor-following logic
 * using `useAccessibilityStore((s) => s.lineGuide)`.
 */
export function LineGuide() {
  return null;
}
